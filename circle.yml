notify:
  webhooks:
    # A list of hook hashes, containing the url field
    - url: https://qa1.kore.com/hooks/7128e0326dc52b1a08847f3121bcffb6

general:
  branches:
    only:
      - master # list of branches to build
      
general:
  build_dir: api
    
