notify:
  webhooks:
    # A list of hook hashes, containing the url field
    - url: https://qa1.kore.com/hooks/01fc427a54a8969b5bd19c474e9f9cf0

general:
  branches:
    only:
      - master # list of branches to build
      
general:
  build_dir: api
    
