notify:
  webhooks:
    # A list of hook hashes, containing the url field
    - url: https://qa1.kore.com/hooks/0f919baf8fdfaf26e13bb1495016ae14

general:
  branches:
    only:
      - master # list of branches to build
      
general:
  build_dir: api
    
